<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Style x:Key="QuickSettingsToggleButton" TargetType="{x:Type ToggleButton}">
    <Setter Property="Foreground" Value="{DynamicResource TextBoxForegroundActive}" />
    <Setter Property="Opacity" Value="0.5" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="VerticalAlignment" Value="Stretch" />
    <Setter Property="Width" Value="{Binding ActualHeight, RelativeSource={RelativeSource Self}}" />
    <Setter Property="Margin" Value="1 0" />
    <Setter Property="Padding" Value="1" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ToggleButton}">
          <Border
            Background="{TemplateBinding Background}"
            BorderBrush="{DynamicResource QuickSettingsToggleButtonBorderBrush}"
            BorderThickness="{DynamicResource QuickSettingsToggleButtonBorderThickness}"
            CornerRadius="{DynamicResource QuickSettingsToggleButtonCornerRadius}">
            <Viewbox
              Margin="{TemplateBinding Padding}"
              Stretch="Uniform"
              StretchDirection="DownOnly">
              <ContentPresenter
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                TextBlock.FontSize="11pt"
                TextBlock.Foreground="{TemplateBinding Foreground}"
                TextBlock.TextAlignment="Center" />
            </Viewbox>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Opacity" Value="0.7" />
        <Setter Property="Background" Value="{DynamicResource QuickSettingsToggleButtonBackgroundHover}" />
        <Setter Property="BorderBrush" Value="{DynamicResource QuickSettingsToggleButtonBorderHover}" />
      </Trigger>
      <Trigger Property="IsChecked" Value="True">
        <Setter Property="Opacity" Value="1" />
        <Setter Property="Background" Value="{DynamicResource QuickSettingsToggleButtonBackgroundActive}" />
        <Setter Property="BorderBrush" Value="{DynamicResource QuickSettingsToggleButtonBorderActive}" />
      </Trigger>
      <Trigger Property="IsEnabled" Value="False">
        <Setter Property="Opacity" Value="0.2" />
      </Trigger>
    </Style.Triggers>
  </Style>
</ResourceDictionary>
